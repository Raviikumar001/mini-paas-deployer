name: nobuild

networks:
  net:
    driver: bridge
    name: nobuild_net   # explicit name keeps DOCKER_NETWORK=nobuild_net stable

volumes:
  buildkit_cache:

services:
  caddy:
    image: caddy:2.11
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
      - "2019:2019"   # admin API — backend patches deployment routes here at runtime
    volumes:
      - ./caddy/config.json:/config/caddy.json:ro
    networks:
      - net
    command: caddy run --config /config/caddy.json
    # No depends_on: caddy is the ingress and starts independently.
    # It will proxy-fail gracefully until backend/frontend are up.

  buildkit:
    image: moby/buildkit:latest
    container_name: buildkit
    restart: unless-stopped
    privileged: true
    networks:
      - net
    volumes:
      - buildkit_cache:/var/lib/buildkit

  backend:
    build:
      context: ./backend
    restart: unless-stopped
    networks:
      - net
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./data:/data
    environment:
      DATABASE_PATH: /data/db.sqlite
      CADDY_ADMIN: http://caddy:2019
      DOCKER_NETWORK: nobuild_net
      BUILDKIT_HOST: docker-container://buildkit
      PORT: 3001
      BASE_DOMAIN: "${BASE_DOMAIN:-localhost}"
    depends_on:
      - caddy
      - buildkit

  frontend:
    build:
      context: ./frontend
    restart: unless-stopped
    networks:
      - net
